server {
    server_name  localhost;

    listen 7070 http2;
    root /app/web;
    index index.html;
    include /etc/nginx/mime.types;

    location / {
        try_files $uri $uri/ /index.html;
    }
    # location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
    #     expires 1y;
    #     add_header Cache-Control "public, max-age=31536000, immutable";
    # }

    # # Security headers
    # add_header Keep-Alive "timeout=300, max=1000" always;
    # add_header X-Frame-Options "SAMEORIGIN" always;
    # add_header X-XSS-Protection "1; mode=block" always;
    # add_header X-Content-Type-Options "nosniff" always;
    # add_header X-Permitted-Cross-Domain-Policies "none" always;
    # add_header X-DNS-Prefetch-Control "on" always;
    # add_header X-Download-Options "noopen" always;
    # add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    # add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), autoplay=(), display-capture=(), fullscreen=(), payment=(), usb=(), midi=(), speaker=(), vibrate=()" always;
    # add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    # # add_header Content-Security-Policy "default-src 'self' 'unsafe-inline' 'unsafe-eval'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https:; style-src 'self' 'unsafe-inline' https:; img-src 'self' data: https:; font-src 'self' data: https:; connect-src 'self' https:;" always;

    # # Performance
    # aio threads;
    # sendfile_max_chunk 1m;
    # http2_max_requests 1000;

    # # TCP
    # sendfile on;
    # tcp_nopush on;
    # tcp_nodelay on;

    # # Buffer sizes
    # client_body_buffer_size 128k;
    # client_header_buffer_size 4k;
    # large_client_header_buffers 4 32k;
    # client_max_body_size 32m;

    # # Timeouts
    # keepalive_requests 5000;
    # keepalive_timeout 300s;
    # client_body_timeout 300s;
    # client_header_timeout 300s;
    # send_timeout 300s;

    # # File cache
    # open_file_cache max=200000 inactive=300s;
    # open_file_cache_valid 300s;
    # open_file_cache_min_uses 3;
    # open_file_cache_errors on;

    # # Gzip compression
    # gzip on;
    # gzip_static on;
    # gzip_comp_level 5;
    # gzip_min_length 256;
    # gzip_proxied any;
    # gzip_vary on;
    # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}