server {
    server_name localhost;
    listen 7070;
    root /app/web;
    index index.html;
    include /etc/nginx/mime.types;
    location / {
        try_files $uri $uri/ /index.html;
    }
}