FROM ghcr.io/cirruslabs/flutter:3.41.5
    
RUN apt-get update && export DEBIAN_FRONTEND=noninteractive && apt-get -y install --no-install-recommends htop clang cmake ninja-build pkg-config libgtk-3-dev libstdc++-12-dev

